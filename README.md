# SDLSpaceShooter

基于 SDL2 的 2D 太空射击游戏。

## 环境要求

- CMake >= 3.20
- C++17 兼容编译器（MSVC / GCC / Clang）
- SDL2、SDL2_image、SDL2_ttf、SDL2_mixer

## 编译

```bash
mkdir build && cd build
cmake ..
cmake --build .
```